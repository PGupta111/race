# Core — always required
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
python-multipart==0.0.9
aiofiles==24.1.0
httpx==0.27.2

# ML / Computer vision — lightweight for Vercel (<150MB)
# Ultralytics and EasyOCR removed to avoid 1GB PyTorch dependency
opencv-python-headless>=4.10.0

# Hardware — install only when using an Intel RealSense depth sensor
# pip install pyrealsense2
